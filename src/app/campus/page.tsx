import { CampusLanding } from "@/components/campus/CampusLanding";

/**
 * Feature: dinee-campus (Task 28.1)
 *
 * Route `/campus` — the student landing page (Req 1.1–1.6, 14.1, 14.2).
 */
export default function CampusLandingPage() {
  return <CampusLanding />;
}
