import { SocialHub } from "@/components/campus/social/SocialHub";

/**
 * Feature: campus-social-loops (Task 19.1)
 *
 * Route `/campus/social` — the social hub linking to the six engagement
 * screens (Agent Battles, Daily Challenges, Share Clips, Group Chat Mode,
 * Streaks & Badges, Campus Quests).
 */
export default function CampusSocialPage() {
  return <SocialHub />;
}
